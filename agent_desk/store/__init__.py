"""SQLite: threads, blocks, ideas, drafts, viewers — and the redaction boundary.

Redaction runs here rather than in a template, because a view that forgets to call a filter renders
correctly and leaks (docs/07-security.md).
"""
