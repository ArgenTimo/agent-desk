"""agent-desk — a read-first console over the Claude Code sessions running on this machine.

The one rule everything else follows: reading a session costs nothing, writing to one costs its
context. See docs/adr/0002-read-first-never-interrupt.md.
"""
